import { DateNoTimezonePipe } from './date-no-timezone-pipe';

describe('DateNoTimezonePipe', () => {
  it('create an instance', () => {
    const pipe = new DateNoTimezonePipe();
    expect(pipe).toBeTruthy();
  });
});
